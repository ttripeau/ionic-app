import { EventEmitter } from '../../../stencil-public-runtime';
import { JeepPhotoViewerResult } from '../../../interfaces/interfaces';
export declare class JeepPhotoShare {
  el: HTMLJeepPhotoShareElement;
  /**
   * The photo url to share
   */
  surl: string;
  /**
   * The photo title to share
   */
  stitle: string;
  /**
   * The text to share
   */
  stext: string;
  innerUrl: string;
  innerTitle: string;
  innerText: string;
  parseUrl(newValue: string): void;
  parseTitle(newValue: string): void;
  parseText(newValue: string): void;
  /**
   * Emitted when the close button was clicked
   */
  onPhotoShareCompleted: EventEmitter<JeepPhotoViewerResult>;
  /**
   * Method initialize
   */
  init(): Promise<void>;
  /**
   * Set the Share.
   */
  setShare(): Promise<void>;
  _element: any;
  _window: Window | any;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): Promise<void>;
  private _init;
  private _setShare;
  render(): any;
}
