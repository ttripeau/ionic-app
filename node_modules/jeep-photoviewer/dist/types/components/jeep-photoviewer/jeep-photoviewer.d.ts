import { EventEmitter } from '../../stencil-public-runtime';
import { Image, ViewerOptions, JeepPhotoViewerResult } from '../../interfaces/interfaces';
export declare class JeepPhotoviewer {
  el: HTMLJeepPhotoviewerElement;
  /**
   * The Image List
   */
  imageList: Image[];
  /**
   * The photoviewer options
   */
  options: ViewerOptions;
  /**
   * The photoviewer mode ("gallery","slider","one")
   */
  mode: string;
  /**
   * The photoviewer image index for mode ("slider","one")
   */
  startFrom: number;
  innerImageList: Image[];
  innerOptions: ViewerOptions;
  innerMode: string;
  innerStartFrom: number;
  showHScroll: boolean;
  close: boolean;
  parseImageList(newValue: Image[]): void;
  parseOptions(newValue: ViewerOptions): void;
  parseMode(newValue: string): void;
  parseStartFrom(newValue: number): void;
  /**
   * Emitted when successful or when an error occurs or a message to be sent
   */
  onPhotoViewerResult: EventEmitter<JeepPhotoViewerResult>;
  handleWindowResize(): void;
  handleJeepPhotoHscrollClose(): Promise<void>;
  handleJeepPhotoButtonsClose(event: CustomEvent): Promise<void>;
  handleJeepPhotoHscrollResult(event: CustomEvent): void;
  /**
   * Method initialize
   */
  init(): Promise<void>;
  closePhotoHScroll(): Promise<void>;
  _isScale: boolean;
  _element: any;
  _window: Window | any;
  _selPos: number;
  _backRange: string[];
  componentWillLoad(): Promise<void>;
  private _init;
  private _windowResize;
  private _setProperties;
  private _handleClick;
  private _closeHScroll;
  render(): any;
}
