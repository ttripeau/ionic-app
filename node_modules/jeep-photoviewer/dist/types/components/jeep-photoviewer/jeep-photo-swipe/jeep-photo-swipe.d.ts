/// <reference types="node" />
import { EventEmitter } from '../../../stencil-public-runtime';
import { Point } from '../../../interfaces/interfaces';
import { IJeepSwipeEvent } from "../../../interfaces/interfaces";
export declare class JeepPhotoSwipe {
  el: HTMLJeepPhotoSwipeElement;
  /**
   * The swipe timeThreshold
   */
  timeThreshold: number;
  /**
   * The swipe threshold in x direction
   */
  thresholdX: number;
  /**
 * The swipe threshold in y direction
 */
  thresholdY: number;
  innerTimeThreshold: number;
  innerThresholdX: number;
  innerThresholdY: number;
  parseTimeThreshold(newValue: number): void;
  parseThresholdX(newValue: number): void;
  parseThresholdY(newValue: number): void;
  /**
   * Emitted when the user is making a swipe gesture
   */
  onPhotoSwipe: EventEmitter<IJeepSwipeEvent>;
  /**
   * Method initialize
   */
  init(): Promise<void>;
  /**
   * handleTouchStart
   */
  handleTouchStart(e: any): Promise<void>;
  /**
   * handleTouchEnd
   */
  handleTouchEnd(e: TouchEvent): Promise<void>;
  /**
   * handleMouseDown
   */
  handleMouseDown(e: any): Promise<void>;
  /**
   * handleMouseEUp
   */
  handleMouseUp(e: any): Promise<void>;
  _element: any;
  _window: Window | any;
  _photoEl: HTMLDivElement;
  _startPoint: Point;
  _endPoint: Point;
  _time: NodeJS.Timer;
  _totalTime: number;
  componentWillLoad(): Promise<void>;
  private _init;
  private _getSwipeDirection;
  render(): any;
}
