import { EventEmitter } from '../../../stencil-public-runtime';
export declare class JeepPhotoButtons {
  el: HTMLJeepPhotoButtonsElement;
  /**
   * Share button visible
   */
  share: string;
  /**
   * Mode "fullscreen" / "normal"
   */
  viewmode: string;
  /**
   * Close button visible
   */
  closebutton: string;
  /**
   * From component
   */
  fromcomponent: string;
  innerShare: string;
  innerViewmode: string;
  innerCloseButton: string;
  innerFromComponent: string;
  visible: boolean;
  mode: string;
  parseShare(newValue: string): void;
  parseViewmode(newValue: string): void;
  parseCloseButton(newValue: string): void;
  parseFromComponent(newValue: string): void;
  /**
   * Emitted when the close button was clicked
   */
  onPhotoButtonsClose: EventEmitter<{
    component: string;
  }>;
  /**
   * Emitted when the fullscreen mode button was clicked
   */
  onPhotoRequestFullscreen: EventEmitter<void>;
  /**
   * Emitted when the minimize mode button was clicked
   */
  onPhotoRequestMinimize: EventEmitter<void>;
  /**
   * Emitted when the share button was clicked
   */
  onPhotoButtonsShare: EventEmitter<void>;
  /**
   * Method initialize
   */
  init(): Promise<void>;
  /**
   * Set the Photo Buttons.
   */
  setPhotoButtons(): Promise<void>;
  /**
   * Set the Navigation Visibility (visible/hidden)
   */
  setJeepPhotoButtonsVisibility(state: boolean): Promise<void>;
  _element: any;
  _window: Window | any;
  _photoButtonsEl: HTMLDivElement;
  _shareEl: HTMLDivElement;
  _modeEl: HTMLDivElement;
  _closeEl: HTMLDivElement;
  _modeUrl: string;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): Promise<void>;
  private _init;
  private _setPhotoButtons;
  private _handleClick;
  render(): any;
}
