import { EventEmitter } from '../../../stencil-public-runtime';
import { Point, Rect } from '../../../interfaces/interfaces';
export declare class JeepPhotoZoom {
  el: HTMLJeepPhotoZoomElement;
  /**
   * The photo url
   */
  url: string;
  /**
   * The maximum zoom scale
   */
  maxzoomscale: number;
  innerUrl: string;
  innerMaxZoomScale: number;
  parseUrl(newValue: string): void;
  parseMaxZooScale(newValue: number): void;
  /**
   * Emitted when the close button was clicked
   */
  onPhotoZoomOneTap: EventEmitter<void>;
  /**
   * Method initialize
   */
  init(): Promise<void>;
  /**
   * Set the Photo.
   */
  setPhoto(): Promise<void>;
  _element: any;
  _window: Window | any;
  _photoEl: HTMLDivElement;
  _imageEl: HTMLImageElement;
  _tapedTwice: boolean;
  _tapedTime: any;
  _tapNum: number;
  _timerStart: boolean;
  _imageNatural: Rect;
  _view: Rect;
  _imageInView: Rect;
  _curZoomScale: number;
  _maxZoomScale: number;
  _curPan: Point;
  _startPoint: Point;
  _movePoint: Point;
  _centerPoint: Point;
  _lastPoint: Point;
  _doubleTouch: Boolean;
  _touchStart: boolean;
  _touchMove: boolean;
  componentWillLoad(): Promise<void>;
  componentDidLoad(): Promise<void>;
  private _init;
  private _setPhoto;
  private setImage;
  private _setHostProperties;
  private _calculateImageInView;
  private _calculateMaxZooScale;
  private _getTouchPoint;
  private _startHandler;
  private _moveHandler;
  private _endHandler;
  private _handleSingleTap;
  private _handleDoubleTap;
  render(): any;
}
